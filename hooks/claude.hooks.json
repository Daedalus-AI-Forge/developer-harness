{
  "description": "developer-harness guard hooks (Claude Code dialect): secrets, large files, merge markers, and the quality gate run before git commit. Declared explicitly in .claude-plugin/plugin.json; deliberately NOT named hooks/hooks.json, which other tools (Codex) auto-discover and parse with their own schema — see hooks/codex.hooks.json and hooks/README.md.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/secret-scan.sh"
          },
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-large-files.sh"
          },
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-merge-markers.sh"
          },
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/quality-gate.sh"
          }
        ]
      }
    ]
  }
}
