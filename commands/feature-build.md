---
description: Build one scoped feature through the gated team pipeline (plan gate → implement → conformance review → verification) using the feature-build skill.
argument-hint: [feature description or design doc]
---

Use the feature-build skill on the given target.

Target: $ARGUMENTS
